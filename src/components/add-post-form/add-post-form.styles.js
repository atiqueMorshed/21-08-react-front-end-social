import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  AddPostFormContainer: {
    width: "80%",
    margin: "auto",
    background: "#e3e3e3",
  },
  AddPostFormContainerDivInput: {
    width: "100%",
    borderRadius: 8,
  },
}));

export default useStyles;
